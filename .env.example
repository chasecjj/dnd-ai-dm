# === REQUIRED ===
DISCORD_BOT_TOKEN=
GEMINI_API_KEY=
GAME_TABLE_CHANNEL_ID=
WAR_ROOM_CHANNEL_ID=
MODERATOR_LOG_CHANNEL_ID=

# === PLAYER MAP (required for gameplay) ===
# Format: discord_username:CharacterName,discord_username2:CharacterName2
# Use the player's Discord username (lowercase, not their display name)
PLAYER_MAP=

# === OPTIONAL: Foundry VTT ===
# Enables live battle maps, dice rolling, token placement, and character sync.
# Requires the Three Hats Relay running (see foundryvtt-relay/docker-compose.yml).
# FOUNDRY_API_KEY=
# FOUNDRY_RELAY_URL=http://localhost:3010
# FOUNDRY_CLIENT_ID=ai-dm

# === OPTIONAL: MongoDB ===
# Enables mechanical state storage (HP, conditions, quest status) separate from
# the Obsidian vault. Without this, the bot runs in vault-only mode — works fine.
# MONGODB_URI=mongodb://localhost:27017/dnd_ai_dm
